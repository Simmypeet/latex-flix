\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{flix}[2026/04/24 Flix]

\RequirePackage{listings}
\RequirePackage{xcolor}

\definecolor{linenumber-color}{RGB}{170, 170, 170}
\definecolor{code-color}{RGB}{0, 0, 0}
\definecolor{keyword-color}{RGB}{155, 70, 147}
\definecolor{effect-keyword-color}{RGB}{0, 116, 102}
\definecolor{type-color}{RGB}{72, 118, 158}
\definecolor{comment-color}{RGB}{55, 55, 55}
\definecolor{string-color}{RGB}{30, 126, 19}

\lstdefinelanguage{flix}{
  basicstyle        = \ttfamily\footnotesize\color{code-color},
  numberstyle       = \footnotesize\color{linenumber-color},
  keywordstyle      = [1]\color{keyword-color}\bfseries,
  keywordstyle      = [2]\color{effect-keyword-color}\bfseries,
  commentstyle      = \color{comment-color},
  identifierstyle   = \color{black},
  stringstyle       = \color{string-color},
  emph              = [1]{Unit, Bool, Char, Float32, Float64, Int8, Int16, Int32, Int64, String},
  emphstyle         = [1]{\color{type-color}},
  showspaces        = false,
  showstringspaces  = false,
  tabsize           = 4,
  numbers           = left,
  xleftmargin       = 0.5cm,
  morecomment       = [l]{//},
  morecomment       = [n]{/*}{*/},
  morestring        = [b]",
  morestring        = [b]',
  morestring        = [b]""",
  morekeywords      = [1]{
    alias,
    and,
    as,
    case,
    catch,
    checked_cast,
    checked_ecast,
    choose,
    def,
    discard,
    eff,
    else,
    ematch,
    enum,
    false,
    fix,
    forA,
    forall,
    force,
    foreach,
    forM,
    from,
    handler,
    if,
    import,
    inject,
    instance,
    instanceof,
    into,
    law,
    lawful,
    lazy,
    let,
    match,
    mod,
    mut,
    new,
    not,
    null,
    open_variant,
    open_variant_as,
    or,
    override,
    par,
    pquery,
    project,
    psolve,
    pub,
    query,
    redef,
    region,
    restrictable,
    run,
    rvadd,
    rvand,
    rvnot,
    rvsub,
    sealed,
    select,
    solve,
    spawn,
    static,
    Static,
    struct,
    super,
    throw,
    trait,
    true,
    try,
    type,
    unchecked_cast,
    Univ,
    unsafe,
    use,
    where,
    with,
    xor,
    xvar,
    yield
  },
  morekeywords      = [2]{
    Channel,
    Clock,
    Console,
    Env,
    Exec,
    File,
    Fs,
    Http,
    IO,
    Logger,
    NonDet,
    Random,
    Stderr,
    Stdin,
    Stdout
  },
}
